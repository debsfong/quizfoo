import React from 'react';
import QuestionsHeader from './questions_header';
import QuestionCards from './question_cards';

// <QuestionsHeader />
const Questions = ({children}) => (
  <div>
    {children}
  </div>
);

export default Questions;
